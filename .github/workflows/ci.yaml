name: CloudMart CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      - name: Checking out repository
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "CloudMart CI has started"

      - name: Run tests
        run: python3 -m pytest
